# Docs Notes

## App Consolidation

This repository now has a single canonical Next.js app at the project root.
The legacy `lore-app` duplicate was retired to prevent route/content drift.

When updating docs, routes, content models, or contributor instructions, reference
the root app structure (`app`, `components`, `content`, `lib`, `scripts`).
